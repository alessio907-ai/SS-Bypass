# ============================================
# Vollständiger PC JAR Scan (ohne C:\)
# Nur Anzeige – nichts wird gelöscht
# ============================================

Write-Host "=== Multi-Laufwerk JAR Scan (kein C:) ===" -ForegroundColor Cyan
Write-Host "Scannt alle Laufwerke außer C:\ für verdächtige JAR-Dateien" -ForegroundColor Yellow
Write-Host ""

# Basisnamen (Beginnt-mit)
$baseNames = @(
    "meteorclient","wurstclient","argon","krypton","catlean",
    "betterping","clickcrystal","killaura","crystalaura",
    "autoclick","triggerbot","aimassist","autocrystal",
    "wtap","tpaura","autototem","playeresp","storageesp",
    "system"
)
$patterns = $baseNames | ForEach-Object { "^$_.*" }

$foundItems = @()
$totalCount = 0

# Alle Laufwerke außer C: holen
$drives = Get-PSDrive -PSProvider FileSystem | Where-Object { $_.Root -ne "C:\" }

foreach ($drive in $drives) {
    Write-Host "Scanne Laufwerk: $($drive.Root)" -ForegroundColor Gray

    try {
        # Rekursiver Scan für .jar, Fehler ignorieren
        Get-ChildItem $drive.Root -Recurse -Filter *.jar -ErrorAction SilentlyContinue | ForEach-Object {
            $totalCount++
            $jarName = $_.Name.ToLower()

            foreach ($pattern in $patterns) {
                if ($jarName -match $pattern) {
                    $foundItems += [PSCustomObject]@{
                        JAR   = $_.Name
                        Größe = "{0:N2} MB" -f ($_.Length / 1MB)
                        Pfad  = $_.FullName
                    }
                    break
                }
            }
        }
    }
    catch {
        # Fehler ignorieren
    }
}

Write-Host ""
Write-Host "=== ERGEBNIS ===" -ForegroundColor Cyan
Write-Host "Gescannt: $totalCount JAR-Dateien"
Write-Host "Verdächtig: $($foundItems.Count)"

if ($foundItems.Count -gt 0) {
    $foundItems | Sort-Object Pfad -Unique | Format-Table -AutoSize
} else {
    Write-Host "Keine passenden Dateinamen gefunden." -ForegroundColor Green
}

Write-Host ""
Write-Host "Scan abgeschlossen. Es wurde NICHTS verändert." -ForegroundColor Yellow
